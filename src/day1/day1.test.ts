import { q1 } from './data'
import { puzzle1 } from './puzzle1'

describe('day 1 puzzles', () => {
  describe('puzzle #1', () => {
    test('boilerplate test', () => {
      expect(puzzle1(q1)).toEqual(15)
    })
  })
})
